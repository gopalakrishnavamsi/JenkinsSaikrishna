//To Create globals for tests -- Define assert/chai/ etc as we go
//require('./globals'); - after creating globals.js

var moduleAlias = require("module-Alias");

moduleAlias.addPath(__dirname + "/../pkg/main/default");
