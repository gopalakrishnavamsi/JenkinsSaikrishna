({
  initializeNewVersionWidget: function (component, event, helper) {
    helper.initializeNewVersionWidget(component, event, helper);
  },

  cancelButtonClicked: function (component, event, helper) {
    helper.close(component);
  },

  uploadButtonClicked: function (component, event, helper) {
    helper.uploadButtonClicked(component, event, helper);
  }
});
