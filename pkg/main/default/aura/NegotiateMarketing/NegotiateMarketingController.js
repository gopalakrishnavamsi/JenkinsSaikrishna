({
  onInitializeComponent: function (component, event, helper) {
    helper.onInitializeComponent(component);
  },

  toggleGenFeaturesSection: function (component, event, helper) {
    helper.toggleGenFeaturesSection(component);
  },

  toggleNegotiateFeaturesSection: function (component, event, helper) {
    helper.toggleNegotiateFeaturesSection(component);
  },

  showGenTrialModal: function (component, event, helper) {
    helper.showGenTrialModal(component);
  },

  showNegotiateTrialModal: function (component, event, helper) {
    helper.showNegotiateTrialModal(component);
  }
});